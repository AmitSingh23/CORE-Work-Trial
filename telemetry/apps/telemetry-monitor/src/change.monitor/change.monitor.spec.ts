describe('ChangeMonitor', () => {
  // it.each([[72,75], 0.2, true]) ('name', (arr: number[], threshold: number, result: boolean) => {}
  it('should returtn true', () => {
    expect(true).toEqual(true);
  });
});
